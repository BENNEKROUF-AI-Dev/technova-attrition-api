# ── API ──────────────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0

# ── Machine Learning ─────────────────────────────────────
scikit-learn>=1.4,<2.0
pandas>=2.2,<3.0
numpy>=1.26,<3.0
joblib>=1.3,<2.0
shap>=0.45,<1.0          # interprétabilité (feature importance) du projet 4

# ── Base de données PostgreSQL ───────────────────────────
SQLAlchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0

# ── Sécurité / authentification ──────────────────────────
python-jose[cryptography]>=3.3,<4.0   # JWT
passlib[bcrypt]>=1.7,<2.0             # hash des mots de passe

# ── Tests ────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
httpx>=0.27,<1.0          # client de test pour l'API FastAPI

# ── Configuration ────────────────────────────────────────
python-dotenv>=1.0,<2.0

imbalanced-learn>=0.12,<1.0

SQLAlchemy>=2.0,<3.0
   psycopg2-binary>=2.9,<3.0

# Note : pour figer les versions exactes une fois l'environnement stable,
#        lancez `pip freeze > requirements.lock.txt`.
