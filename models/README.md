# models/

Ce dossier accueille les artefacts de modèles entraînés (`.joblib` / `.pkl`).

Ces fichiers ne sont **pas versionnés** (voir `.gitignore`) car ils sont
lourds et régénérables : ils sont produits par `ml/train.py`. Ce README
sert uniquement à conserver le dossier dans Git.
